import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Terms & Condition | P.H Forum",
};

const TermsPage = () => {
  return (
    <div>
      <h1>Terms & Condition</h1>
    </div>
  );
};

export default TermsPage;
