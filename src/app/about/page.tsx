import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "About | P.H Forum",
};

const AboutPage = () => {
  return (
    <div>
      <h1>About</h1>
    </div>
  );
};

export default AboutPage;
